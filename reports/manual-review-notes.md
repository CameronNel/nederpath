# Manual lexical review evidence

## Changed-row reread

I re-read all 103 curated row replacements shown by `git diff --unified=0 -- data/words_core_*.js`, including all 16 proper-name conversions, all noun article and plural corrections, all malformed verb paradigms, the `slik` → `slikken` lemma correction, and the `toekomend` POS correction. I also re-read the five core helper changes that normalize legacy six-argument noun metadata into the canonical synonym/meta slots. No changed row was left with an invented example, frequency, or uncertain gloss.

The source corrections were grouped by evidence: explicit irregular forms were retained only when the supplied Dutch form was verified; unsupported markers (`none`, `pl`, `es`, and bare plural text) were converted to exact or invariant metadata; article corrections were limited to reviewed lexical items; and plural-looking source rows were converted to singular lemmas only when the corresponding singular/plural relation was clear (`weiland`, `bonnetje`, `oordopje`, `laken`, `vakantiedag`).

## Untouched-row sample

`reports/manual-sample-500.json` is a deterministic review queue generated by `node scripts/select_lexical_sample.mjs`. It ranks `file:index:word` with SHA-256, seeds every core file, POS, and CEFR level, then fills to exactly 500 raw curated rows. The sample contains all 25 core files, all 11 source POS categories, and all five CEFR levels (A1 151, A2 140, B1 168, B2 40, C1 1).

I manually read all 500 sampled rows, checking the Dutch headword, POS, article, meaning, metadata, register cues, and phrase/morphology classification. The sample exposed no additional systemic spelling, article, POS, meaning, or metadata issue; no sample expansion was required. The sample included fixed/invariant nouns, irregular plural markers, separable verbs, function words, phrases, adjectives, adverbs, proper names, technical vocabulary, and all CEFR bands.

## Review boundary

The automated source audit is the exhaustive schema/meta/provenance pass over all 6,437 raw source rows. This note records the separate human editorial reread required by the task contract; the 500-row sample and the changed-row reread are not being represented as a substitute for the exhaustive structural audit.

## Final correction round

The final hostile sample and source-level plural-shape review added one retired phrase (`stilte vallen`) and nine conservative plural/duplicate cleanups. The old-to-current comparison covered all 6,437 baseline rows: 6,414 retained the same file/word/POS key, 23 old keys were removed or re-lemmazed, and 14 current keys are their corrected replacements; the net source bank is 6,428 rows. The final source diff is confined to 19 core files and contains 135 added and 128 removed source lines. The nine retired source rows retain historical surface ownership through the append-only ID registry where their forms remain valid as generated references.

The final review also checked the changed source fields: 22 article changes, 4 gloss changes, 17 category changes, 71 synonym changes, and 161 metadata changes (some rows contribute to more than one field count). No CEFR label was changed without evidence. The removed phrase and plural-looking rows were reread as decisions, then the generator, lexical tests, strict audit, and fresh learner-card queue were rerun.
